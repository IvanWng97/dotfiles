.DS_Store
*.swp
*~

# brew bundle install rewrites the lockfile locally — pure churn
Backup/Brewfile.lock.json

# Claude Code: machine-local state and runtime cruft
claude/.claude/settings.local.json
claude/.claude/settings.json.*.bak
claude/.claude/settings.lock
claude/.claude/.credentials.json

# DeepSeek/reasonix: machine-local subagent state
.deepseek/
